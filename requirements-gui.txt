# Minimal dependencies for the Habithon desktop GUI
PySide6>=6.5,<7

# Pin NumPy to 1.x for fastText compatibility (NumPy 2.x breaks older fastText bindings)
numpy>=1.24,<2.0

# fastText for language identification (use wheel on macOS for reliability)
fasttext-wheel; platform_system == "Darwin"
fasttext>=0.9.2; platform_system != "Darwin"

# Anonymization (Presidio + spaCy)
presidio-analyzer>=2.2
spacy>=3.7,<4
# spaCy language models (install via direct wheels)
# English
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
# German (3.7.0 is the latest available)
https://github.com/explosion/spacy-models/releases/download/de_core_news_sm-3.7.0/de_core_news_sm-3.7.0-py3-none-any.whl
# Multi-language NER (fallback)
https://github.com/explosion/spacy-models/releases/download/xx_ent_wiki_sm-3.7.0/xx_ent_wiki_sm-3.7.0-py3-none-any.whl

# Optional: building a standalone app bundle
pyinstaller>=6,<7

# Parsers for DOCX/PDF/MSG support used by the converter
python-docx>=0.8.11
pdfminer.six>=20221105
extract_msg>=0.48.5
