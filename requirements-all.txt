# Consolidated dependencies for the full HabithonAI toolchain (GUI + preprocessing + anonymization + APIs)
# Install with: pip install -r requirements-all.txt
# After install, run the spaCy model downloads listed at the bottom (or use scripts/setup_all.sh).

# --- GUI & core ---
PySide6>=6.5,<7
numpy>=1.24,<2.0

# fastText (wheel for macOS convenience)
fasttext-wheel; platform_system == "Darwin"
fasttext>=0.9.2; platform_system != "Darwin"

# Document parsers
pdfminer.six>=20221105
python-docx>=0.8.11
extract_msg>=0.48.5

# Anonymization (Presidio + spaCy)
presidio-analyzer>=2.2
spacy>=3.5

# Optional Postgres token vault
psycopg>=3.1

# Deterministic anonymization / API stack
fastapi>=0.110,<1.0
pydantic>=2.5,<3.0
uvicorn[standard]>=0.23,<1.0

# Translation engines (ct2 + Marian/Transformers)
ctranslate2>=3.9.0
sentencepiece>=0.1.99
transformers>=4.38.0
torch>=2.1.0
sacremoses>=0.0.53
huggingface-hub>=0.21.0

# Packaging (optional for desktop build)
pyinstaller>=6,<7

# --- Post-install (not pip-installable as regular packages) ---
# Run these to download spaCy language models used by Presidio & fallback:
#   python3 -m spacy download en_core_web_sm
#   python3 -m spacy download de_core_news_sm
#   python3 -m spacy download xx_ent_wiki_sm

# If you only need a subset (e.g. no translation), you can comment out the large DL packages (torch, transformers,
# ctranslate2, sentencepiece, sacremoses, huggingface-hub) to speed installation.
