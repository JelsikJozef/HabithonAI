# Minimal dependencies for the Habithon desktop GUI
PySide6>=6.5,<7

# Pin NumPy to 1.x for fastText compatibility (NumPy 2.x breaks older fastText bindings)
numpy>=1.24,<2.0

# fastText for language identification (use wheel on macOS for reliability)
fasttext-wheel; platform_system == "Darwin"
fasttext>=0.9.2; platform_system != "Darwin"

# Anonymization (Presidio + spaCy)
presidio-analyzer>=2.2
spacy>=3.7,<4
# NOTE: spaCy language models are installed via scripts/setup_gui.sh using `python -m spacy download`.
# This avoids flaky direct GitHub wheel downloads during `pip install -r requirements-gui.txt`.
# Models ensured by setup script: en_core_web_sm, de_core_news_sm, xx_ent_wiki_sm

# Optional: building a standalone app bundle
pyinstaller>=6,<7

# Parsers for DOCX/PDF/MSG support used by the converter
python-docx>=0.8.11
pdfminer.six>=20221105
extract_msg>=0.48.5

# LLM summarization
openai>=2.38.0,<3.0
python-dotenv>=1.0.1
